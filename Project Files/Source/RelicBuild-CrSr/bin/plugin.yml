name: RelicBuild-CrSr # the name of the plugin.

version: 1.0.1 # the version of the plugin.

main: com.pezapp.relicbuildcrsr.Plugin # the main class of the plugin.

api-version: 1.13

softDepend: [RelicBuild-Core]

# Commands:
commands:
   rb:
      description: Plugin command.
      usage: /<command>
      permission: RelicBuild.basic
      permission-message: You don't have <permission>